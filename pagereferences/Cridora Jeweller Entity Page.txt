import React, { useState, useEffect } from 'react';
import { 
  ShieldCheck, 
  TrendingUp, 
  MapPin, 
  Star, 
  Info, 
  ArrowRight, 
  Wallet, 
  ShoppingBag, 
  History, 
  Users, 
  ChevronRight,
  Package,
  Award,
  Zap,
  Lock,
  ArrowUpRight,
  EyeOff
} from 'lucide-react';

const App = () => {
  const [activeTab, setActiveTab] = useState('schemes');
  const [livePrice, setLivePrice] = useState(7245.50);
  const [isLoggedIn, setIsLoggedIn] = useState(false); // Simulated auth state

  // Mock data for the Jeweller Entity
  const jeweller = {
    name: "Malabar Gold & Diamonds",
    branch: "Kochi Main Outlet",
    rating: 4.9,
    reviews: 1240,
    isVerified: true,
    joinedDate: "Jan 2023",
    totalSavers: "45.2K",
    redemptionSLA: "Under 24 Hours",
    markup: 1.2, // %
    address: "MG Road, Kochi, Kerala",
    description: "One of the largest jewellery chains in the world, specializing in BIS 916 Hallmarked Gold and high-end bridal collections.",
    schemes: [
      { id: 1, name: "GoldNest 11+1 Premium", duration: "12 Months", benefit: "1 Month Bonus", minAmount: 1000, type: "Cumulative", fees: "0% Processing" },
      { id: 2, name: "Elite Marriage Planner", duration: "24 Months", benefit: "0% Making Charges", minAmount: 5000, type: "Fixed Grams", fees: "Limited to Cridora Pro" },
      { id: 3, name: "Akshaya Flexi-Save", duration: "Open Ended", benefit: "Live Rate Booking", minAmount: 100, type: "Fractional", fees: "Market Linked" }
    ],
    products: [
      { id: 101, name: "Traditional Palakka Necklace", weight: "12.5g", price: 98450, img: "https://images.unsplash.com/photo-1599643478518-a784e5dc4c8f?auto=format&fit=crop&q=80&w=400", makingCharges: "8.5%" },
      { id: 102, name: "Temple Work Bangles", weight: "24.0g", price: 184200, img: "https://images.unsplash.com/photo-1535633302704-c02fbcaf8c51?auto=format&fit=crop&q=80&w=400", makingCharges: "12.0%" },
    ]
  };

  const toggleLogin = () => setIsLoggedIn(!isLoggedIn);

  return (
    <div className="min-h-screen bg-slate-50 font-sans text-slate-900 pb-20">
      {/* Top Navigation Bar */}
      <nav className="sticky top-0 bg-white/80 backdrop-blur-md border-b border-slate-200 z-50 px-4 py-3 flex items-center justify-between">
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 bg-amber-500 rounded-lg flex items-center justify-center text-white font-bold">C</div>
          <span className="font-bold text-xl tracking-tight text-slate-800">Cridora</span>
        </div>
        <div className="flex items-center gap-4">
          <div className="hidden md:block text-right">
            <p className="text-[10px] uppercase text-slate-400 font-bold leading-none">Global 916 Price</p>
            <p className="text-sm font-bold text-slate-800">₹{livePrice.toLocaleString()}/g</p>
          </div>
          <button 
            onClick={toggleLogin}
            className={`${isLoggedIn ? 'bg-green-100 text-green-700 border-green-200' : 'bg-slate-900 text-white border-transparent'} border px-4 py-2 rounded-full text-sm font-medium transition-all flex items-center gap-2`}
          >
            {isLoggedIn ? <><ShieldCheck className="w-4 h-4" /> Connected</> : 'Connect Wallet'}
          </button>
        </div>
      </nav>

      {/* Guest Banner */}
      {!isLoggedIn && (
        <div className="bg-amber-500 text-slate-900 text-center py-2 text-xs font-bold px-4">
          👋 You are viewing the public page. Connect wallet to unlock <span className="underline">Cross-Redemption Offers</span> and <span className="underline">0% Making Charge</span> tiers.
        </div>
      )}

      <main className="max-w-6xl mx-auto px-4 py-6 grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        {/* Left Column: Jeweller Info & Market Data */}
        <div className="lg:col-span-2 space-y-6">
          
          {/* Entity Header Card */}
          <div className="bg-white rounded-3xl p-6 border border-slate-200 shadow-sm overflow-hidden relative">
            <div className="absolute top-0 right-0 w-32 h-32 bg-amber-50 rounded-full -mr-16 -mt-16 blur-3xl opacity-50"></div>
            
            <div className="flex flex-col md:flex-row md:items-center justify-between gap-4">
              <div className="flex items-center gap-4">
                <div className="w-20 h-20 bg-slate-100 rounded-2xl flex items-center justify-center border border-slate-200 shadow-inner">
                  <span className="text-2xl font-bold text-slate-400">LOGO</span>
                </div>
                <div>
                  <div className="flex items-center gap-2">
                    <h1 className="text-2xl font-extrabold text-slate-900">{jeweller.name}</h1>
                    {jeweller.isVerified && <ShieldCheck className="w-6 h-6 text-blue-500" fill="currentColor" />}
                  </div>
                  <p className="text-slate-500 flex items-center gap-1 text-sm">
                    <MapPin className="w-3 h-3" /> {jeweller.address}
                  </p>
                  <div className="flex items-center gap-4 mt-2">
                    <span className="flex items-center gap-1 text-sm font-bold bg-green-50 text-green-700 px-2 py-1 rounded-md">
                      <Star className="w-3 h-3 fill-current" /> {jeweller.rating}
                    </span>
                    <span className="text-xs text-slate-400 font-medium underline cursor-pointer">{jeweller.reviews} Reviews</span>
                  </div>
                </div>
              </div>

              <div className="bg-slate-900 text-white p-4 rounded-2xl min-w-[200px]">
                <p className="text-[10px] uppercase text-slate-400 font-bold mb-1 tracking-widest">Live Jeweller Rate</p>
                <div className="flex items-baseline gap-2">
                  <span className="text-2xl font-black">₹{(livePrice * (1 + jeweller.markup/100)).toFixed(2)}</span>
                  <span className="text-green-400 text-xs font-bold flex items-center">
                    <TrendingUp className="w-3 h-3 mr-0.5" /> +0.4%
                  </span>
                </div>
                {isLoggedIn ? (
                   <p className="text-[10px] text-green-400 mt-1 font-bold">✓ Platinum Member Rate Applied</p>
                ) : (
                   <p className="text-[10px] text-slate-400 mt-1">Includes standard platform markup</p>
                )}
              </div>
            </div>

            <div className="mt-8 grid grid-cols-2 md:grid-cols-4 gap-4 border-t border-slate-100 pt-6">
              <div>
                <p className="text-xs text-slate-400 font-semibold uppercase">Active Savers</p>
                <p className="text-lg font-bold text-slate-800">{jeweller.totalSavers}</p>
              </div>
              <div>
                <p className="text-xs text-slate-400 font-semibold uppercase">Settlement SLA</p>
                <p className="text-lg font-bold text-slate-800">{jeweller.redemptionSLA}</p>
              </div>
              <div className={isLoggedIn ? "" : "opacity-40 blur-[1px]"}>
                <p className="text-xs text-slate-400 font-semibold uppercase">X-Redeem Points</p>
                <p className="text-lg font-bold text-slate-800">{isLoggedIn ? '14 Nationwide' : '•••'}</p>
              </div>
              <div>
                <p className="text-xs text-slate-400 font-semibold uppercase">Custody Risk</p>
                <p className="text-lg font-bold text-blue-600 flex items-center gap-1">Distributed <Info className="w-3 h-3" /></p>
              </div>
            </div>
          </div>

          {/* Navigation Tabs */}
          <div className="flex gap-2 p-1 bg-slate-200/50 rounded-xl w-fit">
            {[
              { id: 'schemes', label: 'GoldNest Schemes', icon: Zap },
              { id: 'products', label: 'Products', icon: ShoppingBag },
              { id: 'about', label: 'Entity Profile', icon: Info }
            ].map((tab) => (
              <button
                key={tab.id}
                onClick={() => setActiveTab(tab.id)}
                className={`flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-bold transition-all ${
                  activeTab === tab.id 
                    ? 'bg-white text-slate-900 shadow-sm' 
                    : 'text-slate-500 hover:text-slate-700'
                }`}
              >
                <tab.icon className="w-4 h-4" />
                {tab.label}
              </button>
            ))}
          </div>

          {/* Tab Content */}
          <div className="space-y-4">
            {activeTab === 'schemes' && (
              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                {jeweller.schemes.map((scheme) => (
                  <div key={scheme.id} className="bg-white p-5 rounded-2xl border border-slate-200 hover:border-amber-400 transition-all cursor-pointer group shadow-sm">
                    <div className="flex justify-between items-start mb-4">
                      <div className="w-10 h-10 bg-amber-100 rounded-full flex items-center justify-center text-amber-600">
                        <History className="w-5 h-5" />
                      </div>
                      <span className="bg-slate-100 text-slate-600 px-3 py-1 rounded-full text-[10px] font-bold uppercase">
                        {scheme.type}
                      </span>
                    </div>
                    <h3 className="text-lg font-bold text-slate-900 mb-1">{scheme.name}</h3>
                    
                    <div className="flex gap-4 mb-4">
                      <div>
                        <p className="text-[10px] text-slate-400 uppercase font-bold">Duration</p>
                        <p className="text-sm font-bold">{scheme.duration}</p>
                      </div>
                      <div>
                        <p className="text-[10px] text-slate-400 uppercase font-bold">Benefit</p>
                        <p className="text-sm font-bold text-green-600">{scheme.benefit}</p>
                      </div>
                    </div>

                    {/* Locked Section for Public Page */}
                    <div className={`p-3 rounded-xl border ${isLoggedIn ? 'bg-blue-50 border-blue-100' : 'bg-slate-50 border-slate-100'}`}>
                      <div className="flex justify-between items-center">
                        <p className="text-[10px] text-slate-400 uppercase font-bold">Settlement Fee</p>
                        {isLoggedIn ? (
                          <p className="text-xs font-black text-blue-700">{scheme.fees}</p>
                        ) : (
                          <div className="flex items-center gap-1 text-[10px] font-bold text-slate-400">
                            <Lock className="w-3 h-3" /> Locked
                          </div>
                        )}
                      </div>
                    </div>

                    <div className="flex items-center justify-between border-t border-slate-50 mt-4 pt-4">
                      <p className="text-xs text-slate-500 font-medium">Starts at ₹{scheme.minAmount}</p>
                      <button className="flex items-center gap-1 text-sm font-bold text-slate-900 group-hover:translate-x-1 transition-transform">
                        Explore Plan <ArrowRight className="w-4 h-4" />
                      </button>
                    </div>
                  </div>
                ))}
              </div>
            )}

            {activeTab === 'products' && (
              <div className="grid grid-cols-2 md:grid-cols-3 gap-4">
                {jeweller.products.map((prod) => (
                  <div key={prod.id} className="bg-white rounded-2xl border border-slate-200 overflow-hidden shadow-sm group">
                    <div className="aspect-square bg-slate-100 overflow-hidden relative">
                      <img src={prod.img} alt={prod.name} className="w-full h-full object-cover group-hover:scale-105 transition-transform" />
                      {!isLoggedIn && (
                        <div className="absolute inset-0 bg-slate-900/10 backdrop-blur-[1px] flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity">
                           <span className="bg-white/90 px-3 py-1 rounded-full text-[10px] font-bold shadow-xl flex items-center gap-1">
                             <Lock className="w-3 h-3" /> Login to view Making Charges
                           </span>
                        </div>
                      )}
                    </div>
                    <div className="p-3">
                      <h4 className="font-bold text-sm text-slate-800 line-clamp-1">{prod.name}</h4>
                      <div className="flex justify-between items-center mt-1">
                        <div className="flex flex-col">
                           <span className="text-xs font-bold text-amber-600">{prod.weight}</span>
                           {isLoggedIn && <span className="text-[9px] font-bold text-green-600 mt-0.5">Making: {prod.makingCharges}</span>}
                        </div>
                        <span className="text-sm font-black">₹{prod.price.toLocaleString()}</span>
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            )}
            
            {activeTab === 'about' && (
              <div className="bg-white p-8 rounded-3xl border border-slate-200 shadow-sm space-y-6">
                 <div>
                    <h3 className="text-lg font-bold mb-2">Heritage & Trust</h3>
                    <p className="text-slate-600 leading-relaxed text-sm">{jeweller.description}</p>
                 </div>
                 <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div className="flex gap-3">
                      <div className="w-10 h-10 shrink-0 bg-blue-50 text-blue-600 rounded-lg flex items-center justify-center">
                        <Award className="w-6 h-6" />
                      </div>
                      <div>
                        <h4 className="font-bold text-sm">BIS 916 Certified</h4>
                        <p className="text-xs text-slate-500">Every gram sold is hallmarked and carries a purity certificate.</p>
                      </div>
                    </div>
                    <div className="flex gap-3">
                      <div className="w-10 h-10 shrink-0 bg-amber-50 text-amber-600 rounded-lg flex items-center justify-center">
                        <Users className="w-6 h-6" />
                      </div>
                      <div>
                        <h4 className="font-bold text-sm">Interoperable Partner</h4>
                        <p className="text-xs text-slate-500">Supports Cridora cross-redemption settlement network.</p>
                      </div>
                    </div>
                 </div>

                 {/* Premium Network Info Block */}
                 {!isLoggedIn && (
                   <div className="bg-slate-900 rounded-2xl p-6 text-white text-center">
                      <Lock className="w-8 h-8 text-amber-500 mx-auto mb-3" />
                      <h4 className="font-bold mb-1">Unlock Cridora Network Benefits</h4>
                      <p className="text-xs text-slate-400 mb-4 px-4">See real-time cross-redemption availability and unified settlement fees for this jeweller.</p>
                      <button 
                        onClick={toggleLogin}
                        className="bg-amber-500 text-slate-900 px-6 py-2 rounded-xl text-sm font-black hover:bg-amber-400 transition-colors"
                      >
                        Login to Cridora
                      </button>
                   </div>
                 )}
              </div>
            )}
          </div>
        </div>

        {/* Right Column: Dynamic Action Widgets */}
        <div className="space-y-6">
          
          {/* Main Transaction Widget */}
          <div className="bg-white p-6 rounded-3xl border border-slate-200 shadow-xl relative overflow-hidden">
            <div className="absolute top-0 right-0 p-2">
              <span className="text-[10px] font-bold bg-amber-100 text-amber-700 px-2 py-0.5 rounded-md">LIVE</span>
            </div>
            
            <h2 className="text-xl font-black mb-4">Quick Accumulate</h2>
            
            <div className="space-y-4">
              <div>
                <label className="text-[10px] font-bold text-slate-400 uppercase tracking-wider mb-1 block">Enter Amount</label>
                <div className="relative">
                  <span className="absolute left-4 top-1/2 -translate-y-1/2 font-bold text-slate-400">₹</span>
                  <input 
                    type="number" 
                    placeholder="500" 
                    className="w-full bg-slate-50 border-2 border-slate-100 rounded-xl py-3 pl-8 pr-4 font-black text-xl focus:border-amber-400 outline-none transition-all"
                  />
                </div>
              </div>
              
              <div className="bg-slate-50 rounded-xl p-4 flex justify-between items-center">
                <div>
                  <p className="text-[10px] text-slate-400 font-bold uppercase">Estimated Gold</p>
                  <p className="text-lg font-black text-slate-800">0.0682 g</p>
                </div>
                <div className="text-right">
                  <p className="text-[10px] text-slate-400 font-bold uppercase">Purity</p>
                  <p className="text-sm font-bold text-amber-600">BIS 916</p>
                </div>
              </div>

              <button 
                onClick={!isLoggedIn ? toggleLogin : null}
                className="w-full bg-amber-500 hover:bg-amber-600 text-white font-black py-4 rounded-xl shadow-lg shadow-amber-200 transition-all flex items-center justify-center gap-2"
              >
                <Wallet className="w-5 h-5" /> {isLoggedIn ? 'Buy Now' : 'Login to Buy'}
              </button>
            </div>
          </div>

          {/* Secondary Actions */}
          <div className="space-y-3">
            <button className="w-full bg-white border border-slate-200 p-4 rounded-2xl flex items-center justify-between group hover:bg-slate-50 transition-all">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 bg-blue-50 text-blue-600 rounded-xl flex items-center justify-center">
                  <Package className="w-5 h-5" />
                </div>
                <div className="text-left">
                  <p className="font-bold text-sm">Deposit Physical Gold</p>
                  <p className="text-[10px] text-slate-500">Convert existing gold to grams</p>
                </div>
              </div>
              {isLoggedIn ? <ChevronRight className="w-4 h-4 text-slate-300" /> : <Lock className="w-3 h-3 text-slate-300" />}
            </button>

            <button className="w-full bg-white border border-slate-200 p-4 rounded-2xl flex items-center justify-between group hover:bg-slate-50 transition-all">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 bg-purple-50 text-purple-600 rounded-xl flex items-center justify-center">
                  <ArrowUpRight className="w-5 h-5" />
                </div>
                <div className="text-left">
                  <p className="font-bold text-sm">Apply for Liquidity</p>
                  <p className="text-[10px] text-slate-500">Get instant cash against holdings</p>
                </div>
              </div>
              {isLoggedIn ? <ChevronRight className="w-4 h-4 text-slate-300" /> : <Lock className="w-3 h-3 text-slate-300" />}
            </button>

            <button className="w-full bg-slate-900 text-white p-4 rounded-2xl flex items-center justify-between group hover:bg-slate-800 transition-all">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 bg-slate-800 text-amber-400 rounded-xl flex items-center justify-center border border-slate-700">
                  <Lock className="w-5 h-5" />
                </div>
                <div className="text-left">
                  <p className="font-bold text-sm">Family Vault</p>
                  <p className="text-[10px] text-slate-400">Accumulate with family members</p>
                </div>
              </div>
              {isLoggedIn ? <ChevronRight className="w-4 h-4 text-slate-500" /> : <Lock className="w-3 h-3 text-slate-500" />}
            </button>
          </div>

          {/* Compliance Card */}
          <div className="p-4 bg-slate-100 rounded-2xl border border-slate-200">
            <div className="flex items-start gap-2">
              <ShieldCheck className="w-4 h-4 text-slate-400 shrink-0 mt-0.5" />
              <p className="text-[10px] text-slate-500 font-medium leading-relaxed">
                Cridora provides the ledger & settlement infrastructure. Gold custody remains with <strong>{jeweller.name}</strong> as per local BIS regulations. Transactions are subject to GST and platform settlement fees.
              </p>
            </div>
          </div>

        </div>
      </main>

      {/* Mobile Bottom Bar (Sticky) */}
      <div className="fixed bottom-0 left-0 right-0 bg-white border-t border-slate-200 p-3 md:hidden flex gap-3 z-50">
        <button className="flex-1 bg-slate-100 text-slate-900 font-bold py-3 rounded-xl text-sm">Explore Designs</button>
        <button 
          onClick={!isLoggedIn ? toggleLogin : null}
          className="flex-1 bg-amber-500 text-white font-bold py-3 rounded-xl text-sm"
        >
          {isLoggedIn ? 'Start Saving' : 'Login to Start'}
        </button>
      </div>
    </div>
  );
};

export default App;