import React, { useState } from 'react';
import { 
  LayoutDashboard, 
  Users, 
  TrendingUp, 
  Wallet, 
  ArrowUpRight, 
  ArrowDownLeft, 
  Settings, 
  Bell, 
  Search,
  CheckCircle2,
  Clock,
  AlertCircle,
  MoreVertical,
  Plus,
  BarChart3,
  Globe
} from 'lucide-react';

const App = () => {
  const [activeView, setActiveView] = useState('overview');

  const stats = [
    { label: "Total Digital Liability", value: "14.28 kg", change: "+1.2g today", color: "amber" },
    { label: "Active GoldNest Savers", value: "1,240", change: "+12 this week", color: "blue" },
    { label: "Pending Redemptions", value: "₹4.2L", count: "8 orders", color: "green" },
    { label: "Settlement Balance", value: "₹18.5L", change: "Due in 24h", color: "slate" },
  ];

  const recentTransactions = [
    { id: "TX-901", user: "Arjun K.", type: "Deposit", amount: "5.000g", status: "Verified", time: "2 mins ago" },
    { id: "TX-902", user: "Sarah L.", type: "Redemption", amount: "12.450g", status: "Pending", time: "15 mins ago" },
    { id: "TX-903", user: "Rahul M.", type: "Scheme Payout", amount: "2.100g", status: "Completed", time: "1 hour ago" },
    { id: "TX-904", user: "Priya S.", type: "Cross-Redeem", amount: "8.200g", status: "Settling", time: "3 hours ago" },
  ];

  return (
    <div className="flex min-h-screen bg-slate-50 font-sans text-slate-900">
      {/* Sidebar Nav */}
      <aside className="w-64 bg-slate-900 text-slate-300 hidden lg:flex flex-col border-r border-slate-800">
        <div className="p-6 flex items-center gap-3">
          <div className="w-8 h-8 bg-amber-500 rounded-lg flex items-center justify-center text-white font-bold">C</div>
          <span className="text-xl font-bold text-white tracking-tight">Cridora<span className="text-amber-500 italic text-sm ml-1">Biz</span></span>
        </div>
        
        <nav className="flex-1 px-4 py-4 space-y-1">
          {[
            { id: 'overview', label: 'Dashboard', icon: LayoutDashboard },
            { id: 'savers', label: 'Savers Network', icon: Users },
            { id: 'schemes', label: 'GoldNest Engine', icon: Settings },
            { id: 'settlement', label: 'Settlements', icon: Globe },
            { id: 'analytics', label: 'Analytics', icon: BarChart3 },
          ].map((item) => (
            <button
              key={item.id}
              onClick={() => setActiveView(item.id)}
              className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-bold transition-all ${
                activeView === item.id 
                ? 'bg-amber-500 text-white' 
                : 'hover:bg-slate-800 hover:text-white'
              }`}
            >
              <item.icon className="w-5 h-5" />
              {item.label}
            </button>
          ))}
        </nav>

        <div className="p-4 border-t border-slate-800">
          <div className="bg-slate-800 p-4 rounded-2xl flex items-center gap-3">
            <div className="w-10 h-10 rounded-full bg-slate-700"></div>
            <div className="overflow-hidden">
              <p className="text-xs font-bold text-white truncate">Malabar Admin</p>
              <p className="text-[10px] text-slate-500 truncate">ID: JD-99201</p>
            </div>
          </div>
        </div>
      </aside>

      {/* Main Content */}
      <main className="flex-1 overflow-y-auto">
        <header className="bg-white border-b border-slate-200 px-8 py-4 flex items-center justify-between sticky top-0 z-10">
          <div className="flex items-center gap-4 bg-slate-100 px-4 py-2 rounded-xl w-96">
            <Search className="w-4 h-4 text-slate-400" />
            <input type="text" placeholder="Search users, TXNs, or weights..." className="bg-transparent border-none outline-none text-sm w-full" />
          </div>
          
          <div className="flex items-center gap-4">
            <div className="flex items-center gap-2 px-3 py-1.5 bg-green-50 text-green-700 rounded-full border border-green-100">
              <span className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
              <span className="text-xs font-bold uppercase tracking-wider">Sync: Live</span>
            </div>
            <button className="p-2 bg-slate-100 rounded-full text-slate-600 hover:bg-slate-200 relative">
              <Bell className="w-5 h-5" />
              <span className="absolute top-0 right-0 w-2 h-2 bg-red-500 rounded-full"></span>
            </button>
          </div>
        </header>

        <div className="p-8 max-w-7xl mx-auto">
          <div className="flex items-center justify-between mb-8">
            <div>
              <h2 className="text-3xl font-black text-slate-900">Partner Dashboard</h2>
              <p className="text-slate-500 font-medium">Monitoring Kochi Main Outlet Infrastructure</p>
            </div>
            <button className="bg-slate-900 text-white px-6 py-3 rounded-2xl font-bold flex items-center gap-2 hover:bg-slate-800 transition-shadow shadow-lg">
              <Plus className="w-5 h-5" /> Create New Scheme
            </button>
          </div>

          {/* Stats Grid */}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            {stats.map((stat, i) => (
              <div key={i} className="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm">
                <p className="text-[10px] uppercase font-black text-slate-400 tracking-widest mb-1">{stat.label}</p>
                <h3 className="text-2xl font-black text-slate-900 mb-2">{stat.value}</h3>
                <div className="flex items-center gap-1">
                  <span className={`text-[10px] font-bold px-2 py-0.5 rounded-full ${
                    stat.color === 'amber' ? 'bg-amber-50 text-amber-700' :
                    stat.color === 'blue' ? 'bg-blue-50 text-blue-700' :
                    stat.color === 'green' ? 'bg-green-50 text-green-700' : 'bg-slate-100 text-slate-600'
                  }`}>
                    {stat.change || stat.count}
                  </span>
                </div>
              </div>
            ))}
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Recent Activity Table */}
            <div className="lg:col-span-2 bg-white rounded-3xl border border-slate-200 shadow-sm overflow-hidden">
              <div className="px-6 py-4 border-b border-slate-100 flex items-center justify-between">
                <h3 className="font-bold">Recent Orchestrations</h3>
                <button className="text-xs font-bold text-amber-600 hover:underline">View All</button>
              </div>
              <div className="overflow-x-auto">
                <table className="w-full text-left">
                  <thead>
                    <tr className="bg-slate-50 text-[10px] uppercase font-black text-slate-400">
                      <th className="px-6 py-3">Transaction</th>
                      <th className="px-6 py-3">User</th>
                      <th className="px-6 py-3">Weight</th>
                      <th className="px-6 py-3">Status</th>
                      <th className="px-6 py-3">Time</th>
                    </tr>
                  </thead>
                  <tbody className="divide-y divide-slate-100">
                    {recentTransactions.map((tx) => (
                      <tr key={tx.id} className="hover:bg-slate-50 transition-colors">
                        <td className="px-6 py-4">
                          <p className="text-xs font-bold text-slate-900">{tx.type}</p>
                          <p className="text-[10px] text-slate-400">{tx.id}</p>
                        </td>
                        <td className="px-6 py-4 text-xs font-medium text-slate-600">{tx.user}</td>
                        <td className="px-6 py-4 text-xs font-black text-slate-900">{tx.amount}</td>
                        <td className="px-6 py-4">
                          <span className={`px-2 py-1 rounded-full text-[10px] font-bold ${
                            tx.status === 'Verified' || tx.status === 'Completed' ? 'bg-green-50 text-green-600' :
                            tx.status === 'Pending' ? 'bg-amber-50 text-amber-600' : 'bg-blue-50 text-blue-600'
                          }`}>
                            {tx.status}
                          </span>
                        </td>
                        <td className="px-6 py-4 text-[10px] text-slate-400 font-medium">{tx.time}</td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            </div>

            {/* Side Panel: Scheme Health & Settlement */}
            <div className="space-y-6">
              <div className="bg-slate-900 text-white rounded-3xl p-6 shadow-xl relative overflow-hidden">
                <div className="absolute top-0 right-0 p-4 opacity-20">
                  <Globe className="w-20 h-20" />
                </div>
                <h3 className="text-lg font-bold mb-4 relative z-10">Network Balance</h3>
                <div className="space-y-4 relative z-10">
                  <div className="flex justify-between items-center">
                    <span className="text-xs text-slate-400">Inbound Liability</span>
                    <span className="text-sm font-bold text-red-400">₹4.2L</span>
                  </div>
                  <div className="flex justify-between items-center">
                    <span className="text-xs text-slate-400">Settled Revenue</span>
                    <span className="text-sm font-bold text-green-400">₹12.8L</span>
                  </div>
                  <div className="pt-4 border-t border-slate-800">
                    <button className="w-full bg-amber-500 py-3 rounded-xl text-xs font-black uppercase tracking-widest hover:bg-amber-600 transition-colors">
                      Reconcile Ledger
                    </button>
                  </div>
                </div>
              </div>

              <div className="bg-white rounded-3xl border border-slate-200 p-6 shadow-sm">
                <h3 className="font-bold mb-4">GoldNest Performance</h3>
                <div className="space-y-4">
                  {[
                    { label: "11+1 Premium", count: 420, capacity: 500, color: "amber" },
                    { label: "Marriage Planner", count: 180, capacity: 200, color: "blue" },
                  ].map((scheme, idx) => (
                    <div key={idx}>
                      <div className="flex justify-between text-xs font-bold mb-1">
                        <span>{scheme.label}</span>
                        <span>{Math.round((scheme.count/scheme.capacity)*100)}%</span>
                      </div>
                      <div className="w-full bg-slate-100 h-2 rounded-full">
                        <div 
                          className={`h-full rounded-full ${scheme.color === 'amber' ? 'bg-amber-400' : 'bg-blue-400'}`} 
                          style={{ width: `${(scheme.count/scheme.capacity)*100}%` }}
                        ></div>
                      </div>
                      <p className="text-[10px] text-slate-400 mt-1">{scheme.count} Active Enrolments</p>
                    </div>
                  ))}
                </div>
              </div>

              {/* Quick Alerts */}
              <div className="bg-red-50 border border-red-100 rounded-3xl p-6">
                <div className="flex gap-3">
                  <AlertCircle className="w-5 h-5 text-red-500" />
                  <div>
                    <h4 className="text-sm font-bold text-red-900">Inventory Warning</h4>
                    <p className="text-xs text-red-700 mt-1">Physical 916 gold stock is 5% below digital liability threshold. Please update vault balances.</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  );
};

export default App;